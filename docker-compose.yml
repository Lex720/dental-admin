version: '2'
services:
  db:
    image: mongo
    ports:
      - "27017:27017"
  web:
    build: ./build
    image: python-django
    command: "bash -c 'cd dentaladmin && python manage.py runserver 0.0.0.0:8000'"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db